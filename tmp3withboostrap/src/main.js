import Vue from 'vue'
import App from './App.vue'

import 'bootstrap'
import 'bootstrap/scss/bootstrap.scss' //超慢

import fontawesome from '@fortawesome/fontawesome'

new Vue({
  el: '#app',
  render: h => h(App)
})
